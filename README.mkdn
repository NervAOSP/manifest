NervAOSP
========

Getting started
---------------

Initialize and sync the source:

```
repo init -u https://github.com/NervAOSP/Manifest.git -b 16.2 --git-lfs --depth 1
```
Then sync:
```
repo sync -c --force-sync --no-clone-bundle --no-tags -j$(nproc)
```

Initialize the build environment, then select a supported device target:

# Build

- Set up the build environment
```bash
. b*/env*
```

- Select a target
```bash
unit lineage_codename-bp4a-user
```
```bash
example
unit lineage_courbet-bp4a-userdebug
```

- To start compiling
```bash
fly -j$(nproc --all)
```
