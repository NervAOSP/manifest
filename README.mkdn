NervAOSP
===========

Getting started
---------------

To initialize your local repository using the NervAOSP trees, use a command like this:
```
repo init -u https://github.com/NervAOSP/Manifest.git -b 16.2 --git-lfs --depth 1
```
Then to sync up:
```
repo sync
```

---------------